# requirements.txt
# Crypto Trading Bots — Build v2.0.0 (2025-10-11)
# Pinned for repeatable deploys; safe on Python 3.11+

fastapi>=0.110,<1.0          # API framework (Pydantic v2 compatible)
uvicorn[standard]>=0.23,<1.0 # ASGI server
requests>=2.31,<3            # Kraken REST calls

pandas>=2.0,<3               # Data wrangling for bars
numpy>=1.24,<3               # Math utilities

python-dotenv>=1.0,<2        # (optional) load .env in local dev
