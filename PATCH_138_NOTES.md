# Patch 138 — Dashboard active positions binding fix

- Fixes Active Positions panel binding to authoritative active plans when dashboard route uses build_reconcile_snapshot() without latest_snapshot.
- Read-only only.
- No trading logic changes.
