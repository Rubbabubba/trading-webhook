Patch 098 fixes the dashboard route binding regression.

Changes:
- restores /dashboard to the HTML dashboard handler
- leaves /diagnostics/strategy_performance on its JSON handler
- updates patch marker to patch-098-dashboard-route-fix
